Функции и параметры
Преобработки большинства приложений, их разделяют на функциональные модули. В C# приложение состоит из классов которые содержат именованные блоки кода называемые методами.
(функции это часть класса, который может осуществлять действия или вычеслять значения).
Использование методов. Метод это набор которые выполняются вместе для создания метода необходимо создать его имя, определить список параметров и тело метода. 
Для вызова метода другого класса необходимо, чтобы он был объявлен как паблик. Вызов осуществляется по вызову классов и метода. Если метод не void, то необходимо вернуть знаечние соответствующего
типа. Оператор return останавливает выполнение метода и передаёт управление выпавшему данному методу оператору.Каждый метод имеет набор своих локальных перемен они видны
только в нём и при завершении метода уничтожаються. Для того чтобы переменные были видны в нескольких методах класса, необходимо объявить их в поляне не метода.
Для не void методов необходимо возврощать значение каждой " путь выполнения методадолжен заканчиваться оператором return".Для void методов оператор return необязателен.
Использование параметров.
Параметры позволяют передавать информацию из одного метода в другой. При объявление метода можно задать список его параметров, если список пустой,то это означает что метод не
имеет параметров. В c# муществуют 3 варианта передачи параметров: параметры.
По значению(изменение значение параметра в методе не влияет на значение в вызвовшем методе)
По ссылке(необходимо указывать ключевое слово rep)
Входные параметры
До вызова метода необходимо обязательно иницилировать методы.
C# позваоляет использовать механизм передачи списка параметров меняемой длинны.
Это ключевые слова которые задают объявленый уровень доступности члена или типа.
7 уровней специальных возможностей модификаторов доступа, паблик, протектор, интернал,протектор интернал, private, privat протектор.
В паблике: доступ неограничен 
протектор: доступ ограничен
интернал: доступ ограничен в текущей форме
private: доступ ограничен с содержащим типом
Модификатор абстракт
Модификатор ocynt
